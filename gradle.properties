# Plain console: the rich console's animated progress bar redraws on stderr and
# clobbers the interactive :client REPL (and any stdio output). Plain mode still
# prints build/task output, just without the live progress bar.
org.gradle.console=plain
